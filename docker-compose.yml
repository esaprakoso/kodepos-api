services:
  app:
    build: .
    container_name: node-ts-sqlite

    ports:
      - "3001:3001"

    volumes:
      # hanya data yang writable
      - ./data:/app/data:rw

    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite

    restart: unless-stopped